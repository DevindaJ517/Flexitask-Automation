# FastAPI and Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP Client (version compatible with both telegram and supabase)
httpx>=0.24.0,<0.25.0

# Telegram (use older version compatible with httpx <0.25)
python-telegram-bot==20.6

# Supabase
supabase==2.3.0

# Redis (for job tracking)
redis==5.0.1

# Scheduler (replaces Celery for free deployment)
apscheduler==3.10.4
